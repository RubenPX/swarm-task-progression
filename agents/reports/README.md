# reports/ — informes de entrega del swarm

Un informe por tarea: `informe-<AAAA-MM-DD>-<slug>.md`, escrito por el swarm según el protocolo (`agents/swarm/custom_agent.md` §3) y commiteado como último commit de la entrega.

Reglas:
- NUNCA sobrescribir informes anteriores
- Es lo primero que lee el revisor (`agents/revisor/REVISOR.md`)
- Sin informe, la entrega se considera incompleta
